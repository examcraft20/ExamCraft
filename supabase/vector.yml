api:
  enabled: true
  address: 0.0.0.0:9001

sources:
  postgres_logs:
    type: docker_logs
    include_labels:
      - "com.docker.compose.service=db"

sinks:
  console:
    type: console
    inputs:
      - postgres_logs
    encoding:
      codec: json
