{
  "name": "@examcraft/types",
  "private": true,
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo \"No types lint tasks yet\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No types tests yet\""
  }
}

